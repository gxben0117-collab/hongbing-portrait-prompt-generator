# 2026-05-23 文件檔案整理報告

## 整理目的

昨天資料曾出現混亂，今天透過返回與遠端整合救回後，專案需要重新分清楚正式工程、建置產物、歷史版本與暫存救回檔。

本次整理採保守策略：不刪除、不合併、不改寫歷史，只把未追蹤且容易混淆的檔案集中保存。

## 目前正式工程來源

- `index.html`：正式網頁入口，目前顯示 `v1.0.0 · 核心規範 v1.6`
- `core.js`：prompt engine 與核心互動邏輯
- `scripts/`：已追蹤的正式維護腳本
- `docs/`：正式文件中心
- `核心資料/`：核心規範與風格範例來源
- `versions/`：已追蹤的歷史快照

## 建置與本機環境

- `dist/`：由 `python scripts/build_dist.py` 產生的本機發佈產物，不是主要修改來源
- `.venv/`：本機 Python 環境，不納入產品邏輯
- `temp/`：暫存、驗證、救回或備份用途

## 本次移入暫存保存的檔案

以下未追蹤檔已移入：

```text
temp/recovered-untracked-20260523/
```

保存內容包含：

- `scripts/` 下未追蹤的臨時檢查、審核、修復與同步腳本
- `scripts/` 下未追蹤的審核報告與 review markdown
- 根目錄的 `temp_expand_debug.txt`
- `versions/` 下未追蹤的 pre-sync / pre-expand / pre-theme 備份快照

這些檔案沒有刪除，只是移出正式工作路徑，避免和已追蹤的正式腳本、正式版本快照混在一起。

## 驗證結果

- `git status --short --untracked-files=all`：整理後無未追蹤或未提交項目
- `python scripts/check_structure.py`：通過

## 後續建議

1. 若需要從救回檔找回某個腳本或快照，先從 `temp/recovered-untracked-20260523/` 查。
2. 確認有正式價值的腳本，再移回 `scripts/` 並加入版本控制。
3. 確認不再需要的暫存檔，之後再由使用者明確指定是否清理。
