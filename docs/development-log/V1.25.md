# V1.25

日期：2026-05-22

## 本次更新

1. 完成 `TASK-002`，把 `CATEGORY_POSE_LIBRARY.hanfu` 中最後殘留的 `scholar beauty`、`warrior heroine` 等 archetype 用詞改為完全中性的角色功能描述。
2. 將這層規則正式寫入驗證腳本，新增 `noArchetypeTermsInPoseGuidance` 檢查，避免未來在行為誘導層重新混入 `beauty / heroine` 類詞。
3. 同步更新版本鏈到 `v0.42 / 核心規範 v1.4`，補齊文件與快照。

## 階段結論

現在不只 `TPLS.char` 是中性的，連 `Category pose guidance` 這種較隱性的行為誘導層也已經去 archetype 化。這代表 identity-first 架構不只是限制場景模板，而是連「建議人物怎麼動」的文字都改成不會偷偷把臉帶往模板角色的寫法。
