# V1.26

日期：2026-05-22

## 本次更新

1. 調整首頁「快速隨機」區塊的左右位置，將目前選中的黑色標籤移到左側，藍色 `快速隨機` 按鈕移到右側。
2. 這次只修改 `index.html` 的區塊順序，不變動 `doRandomAndCopy()` 行為與 prompt engine。
3. 同步升版到 `v0.43`，補齊快照與 README 版本清單。

## 階段結論

這是一個純 UI 排版微調，目的在於讓使用者先看到當前隨機結果，再決定是否繼續抽。功能邏輯不變，但視覺閱讀順序更接近實際操作心流。
