# V1.14

日期：2026-05-21

## 本次更新

1. 整理並確認主程式版本已推進至 `v0.31`，同步補齊文件對 `v0.26`、`v0.27`、`v0.28`、`v0.30`、`v0.31` 的版本描述。
2. 補記核心規範 `v1.2` 變更內容，包含：
   - 新增 `CORE_BEAUTY_SAFETY`
   - 擴充 `AVOID_LOCK`
   - 多組妝容改為 `surface cosmetics only`
3. 記錄 `v0.31` 介面強化：
   - 首頁上方按鈕改為「快速隨機風格咒語複製」
   - 下方輸出操作列新增第二個同功能按鈕
   - 兩個入口都支援全庫隨機、生成咒語、自動複製、捲動至結果區
4. 更新文件中心、專案總覽、版本規則與 README，避免文件仍停留在 `V1.10 / v0.25 / 核心規範 v1.1`

## 結果

- 文件狀態已和主程式現況重新對齊
- 後續若繼續追加卡片、調整 UI 或準備上架，文件可直接沿用目前版本鏈往下記錄
