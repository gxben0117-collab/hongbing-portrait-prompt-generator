# V1.27

日期：2026-05-22

## 本次更新

1. 正式執行 `TASK-002`，採用方案 A：硬精簡，從選項層移除高風險焦段、旅遊紀實鏡頭語言、低調暗光、暗黑戲劇與仙氣縹緲。
2. 重建 `TPL_DEFAULTS` 主路徑，將旅拍類收斂到 `50mm + 雜誌封面 + 自然日光 + 晴空清透`，仙俠 / 古裝類收斂到 `50mm + 雜誌封面 + 黃金時刻 + at_misty`。
3. 更新 `ANTI_PATTERNS` 與 `sanitizePromptText()`，新增 `cl_travel` 封鎖與 `ethereal / celestial / travel documentary / low-key dramatic` 等高風險詞的 safer replacement。
4. 更新 `faceDesc` placeholder 與欄位下方說明文字，讓使用者知道哪些臉部特徵最值得優先填寫。
5. 擴充驗證腳本，新增對安全選項集合、淘汰 id、`at_misty` 限制語與 `faceDesc` 引導文字的檢查。

## 階段結論

這一版不是單純刪掉幾個選項，而是把產品從「風格可玩性優先」正式推向「高良率安全主路徑優先」。後續若要討論進階模式，應該建立在這條主幹已經穩定的前提上。
