# V1.29

日期：2026-05-22

## 本次更新

1. 新增第四輪 curated 擴充腳本 `scripts/expand_style_examples_round4_curated_20260522.mjs`，把低庫存分類的補庫流程正式化，不再走直接批次灌入模式。
2. 第四輪只針對目前最缺、又最不容易撞題的 12 個分類補資料：
   - `song_grace`
   - `ming_grace`
   - `qing_grace`
   - `reference_styles`
   - `hotdrama`
   - `china_drama`
   - `spirits`
   - `goddess_myth`
   - `holy_angel`
   - `modern_lady`
   - `queen`
   - `wedding_diamond`
3. 每個分類新增 6 組，共新增 72 組；寫入前會逐筆檢查：
   - 現有 `CATS` 名稱 / 副標是否撞題
   - 名稱詞幹是否過度接近舊卡
   - `核心資料/風格範例.md` 是否已有同題材
4. 第四輪新增內容同步回寫兩處來源：
   - `index.html` 內的 `CATS`
   - `核心資料/風格範例.md`
5. 介面層新增 icon fallback：
   - `renderCatStrip()`
   - `renderPresets()`
   - `renderBadge()`
   都改用 `getDisplayIcon()`，避免旗幟 emoji 在部分系統退化為 `TW` 之類字母碼。
6. `index.html` 的分類列固定提升為 8 行，讓分類數量增加後仍維持可讀性。
7. 第四輪完成後，主要低庫存分類數量更新為：
   - `song_grace`：36
   - `ming_grace`：35
   - `qing_grace`：35
   - `reference_styles`：44
   - `hotdrama`：38
   - `china_drama`：42
   - `spirits`：41
   - `goddess_myth`：42
   - `holy_angel`：40
   - `modern_lady`：41
   - `queen`：38
   - `wedding_diamond`：42
8. 全庫總條目數由 `1844` 提升至 `1916`。
9. 本輪驗證與建置目標：
   - `python scripts/report_data_gaps.py`
   - `python scripts/check_structure.py`
   - `node scripts/verify_identity_engine.mjs`
   - `python scripts/build_dist.py`

## 階段結論

這一輪的重點不是把每個分類一次灌到超大量，而是把「逐筆分析、逐筆補齊、逐筆檢核」真正變成工程流程。資料擴充、前台顯示、文件鏈與版本快照都一起更新，讓後續如果還要做第五輪、第六輪擴充，可以延續同一套可維護的補庫方式，而不是回到人工散改或無差別批次生成。

## 補充文件

同日補上兩份架構文件，作為後續資料治理與核心規範工程化的正式入口：

- `docs/architecture/風格範例資料結構規格 v1.0.md`
- `docs/architecture/核心規範對應工程映射 v1.0.md`

並補上流程文件：

- `docs/process/風格範例 lint 與審核規則 v1.0.md`
- `docs/process/風格範例審核標記格式 v1.0.md`
- `docs/process/風格範例 lint 腳本規格 v1.0.md`
