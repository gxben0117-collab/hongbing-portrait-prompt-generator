# V1.30

日期：2026-05-22

## 本次更新

1. 新增第五輪 curated 擴充腳本 `scripts/expand_style_examples_round5_curated_20260522.mjs`，把全分類補庫正式推進到「分類配額 + 去重 + 禁詞 + 欄位完整化」的一致流程。
2. 第五輪不是直接無差別灌資料，而是依現有 43 個分類重新分配補庫配額：
   - 11 個相對低庫存分類各補 24 組
   - 其餘 32 個分類各補 23 組
   - 合計精準新增 `1000` 組
3. 腳本寫入前會逐筆檢查：
   - `index.html` 內現有 `CATS` 的名稱 / 副標是否撞題
   - 名稱詞幹是否過度接近既有卡片
   - `核心資料/風格範例.md` 是否已有同題材或近似組合
   - 內容是否含 `heroine`、`beauty`、`movie trailer`、`back-facing` 等高風險詞
4. 第五輪新增內容同步回寫兩處來源：
   - `index.html` 內的 `CATS`
   - `核心資料/風格範例.md`
5. UI 同步調整：
   - 風格大類列由 8 行提升為 9 行
   - 既有 `getDisplayIcon()` fallback 繼續沿用到所有新增卡片
   - 驗證結果為新增後 `missingIcons = 0`
6. 第五輪完成後，全庫總條目數由 `1916` 提升至 `2916`。
7. 本輪後的分類數量更新如下：
   - `taiwan_travel`：84
   - `mountain_sea`：80
   - `europe_travel`：81
   - `japan_travel`：88
   - `korea_sea`：83
   - `world_travel`：90
   - `china_mark`：84
   - `hanfu`：104
   - `dynasty_palace`：77
   - `tang_grandeur`：84
   - `song_grace`：59
   - `ming_grace`：58
   - `qing_grace`：58
   - `oriental`：54
   - `reference_styles`：67
   - `xianxia`：64
   - `myth`：55
   - `china_myth_chars`：85
   - `drama`：58
   - `hotdrama`：61
   - `china_drama`：65
   - `three_kingdoms`：55
   - `jinyong`：66
   - `classic_lit`：53
   - `chinese_story`：56
   - `fantasy`：64
   - `gothic`：79
   - `darkfantasy`：71
   - `spirits`：64
   - `water`：60
   - `goddess_myth`：65
   - `holy_angel`：63
   - `fallen_angel`：77
   - `succubus_demon`：82
   - `dragon_beast`：55
   - `beast_tamer`：58
   - `modern_lady`：64
   - `realistic_life`：54
   - `queen`：61
   - `wedding_diamond`：65
   - `cyberpunk_sf`：54
   - `game`：56
   - `cos_character`：55
8. 本輪驗證與建置目標：
   - `python scripts/report_data_gaps.py`
   - `python scripts/check_structure.py`
   - `node scripts/verify_identity_engine.mjs`
   - `python scripts/build_dist.py`

## 階段結論

第五輪的重點不是把資料量撐大而已，而是把「大規模補庫」也納入既有治理模型。這一輪開始，新增 1000 組也必須遵守分類配額、重複檢查、禁詞檢查、欄位完整化與 UI 相容性，不再是脫離工程規範的外掛式資料灌入。
